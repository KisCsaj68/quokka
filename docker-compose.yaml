version: "3.9"

services:
  frontend:
    container_name: frontend
    build: ./frontend/.
    ports:
      - "3000:3000"
  asset-cache:
    container_name: asset_cache
    build: ./assetcache/.
    ports:
      - "5000:5000"